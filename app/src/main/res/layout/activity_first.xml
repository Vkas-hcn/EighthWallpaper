<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/ic_background">

        <ImageView
            android:layout_width="105dp"
            android:layout_height="105dp"
            android:layout_marginBottom="32dp"
            android:src="@drawable/ic_first_logo"
            app:layout_constraintBottom_toTopOf="@+id/progressBar_first"
            app:layout_constraintStart_toStartOf="@+id/progressBar_first" />

        <ProgressBar
            android:id="@+id/progressBar_first"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="200dp"
            android:layout_height="4dp"
            android:layout_marginBottom="44dp"
            android:layout_marginStart="40dp"
            android:progress="55"
            android:progressDrawable="@drawable/pb_bg"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
